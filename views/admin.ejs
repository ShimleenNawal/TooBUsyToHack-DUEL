<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Page</title>

    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/admin.css">
</head>

<body data-userid="<%= userid %>">
    <h2 class ="type-writer"> Welcome, <%= userid %></h2>
    <div class="content-layout">
    <!-- Left column: buttons -->
    <div class="button-column">
        <form action="/admin/<%= userid %>/create" method="POST">
        <button type="submit" class="btn-tasks">Create</button>
        </form>
        <button class="btn-rivals" id="rivalButton">Inbox</button>
    </div>

    <!-- Right column: leaderboard -->
    <div class="leaderboard-column">
        <h2 class="title">Leaderboard</h2>
        <table class="leaderboard">
        <thead>
            <tr>
            <th>Rank</th>
            <th>UserID</th>
            <th>Medals</th>
            </tr>
        </thead>
        <tbody>
            <% topStudents.forEach((student, index) => { %>
            <tr>
            <td><%= index + 1 %></td>
            <td><%= student.id %></td>
            <td><%= student.medals %></td>
            </tr>
            <% }) %>
        </tbody>
        </table>
    </div>
    </div>

    <script src="/javascripts/userpage.js"></script>
</body>
</html>